info:
  name: UPDATE GAME
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/games/:id"
  params:
    - name: id
      value: "3"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Quiz sur les séries modifié 2",
        "userIds": [1,2],
        "quizId": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
