info:
  name: CREATE A GAME
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/games"
  body:
    type: json
    data: |-
      {
        "name": "Quiz sur le cinéma",
        "userIds": [2, 1],
        "quizId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
