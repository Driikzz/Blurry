info:
  name: CREATE A GAME
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/games"
  body:
    type: json
    data: |-
      {
        "name": "Quiz sur les séries",
        "userIds": [2],
        "quizId": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
