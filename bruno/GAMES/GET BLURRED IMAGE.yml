info:
  name: GET BLURRED IMAGE
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/games/test/blurred-image/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
