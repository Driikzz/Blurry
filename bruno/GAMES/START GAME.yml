info:
  name: START GAME
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/games/start/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
