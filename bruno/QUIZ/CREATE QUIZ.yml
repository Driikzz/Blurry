info:
  name: CREATE QUIZ
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/quiz"
  body:
    type: json
    data: |-
      {
        "name": "Quiz spécial cinéma"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
