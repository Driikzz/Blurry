info:
  name: GET QUIZ BY ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/quiz/:id"
  params:
    - name: id
      value: "4"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
