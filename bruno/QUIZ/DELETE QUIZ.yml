info:
  name: DELETE QUIZ
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/quiz/:id"
  params:
    - name: id
      value: "4"
      type: path
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
