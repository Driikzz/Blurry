info:
  name: PUT UPDATE QUIZ
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/quiz/:id"
  params:
    - name: id
      value: "4"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Quiz spécial tout modifié"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
