info:
  name: GET BY ID USER
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/users/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
