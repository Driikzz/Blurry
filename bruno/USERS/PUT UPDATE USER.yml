info:
  name: PUT UPDATE USER
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/users/2"
  body:
    type: json
    data: |-
      {
        "name":"Rémi12",
        "email":"remisalles@gmail.com",
        "password":"aaaaaa"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
