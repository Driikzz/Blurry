info:
  name: ST_LOGIN_ERROR_PASSWORD
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email" : "test@gmail.com",
        "password" : "jesuisunfauxmotdepasse"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("test email not found", function () {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
