info:
  name: ST_LOGIN_ERROR_EMAIL
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email" : "wrong@email.fr",
        "password" : "jesuisfortdefou"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("test email not found", function () {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
