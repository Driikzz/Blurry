info:
  name: ERROR-REGISTER-NOT-EMAIL
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/auth/register"
  body:
    type: json
    data: |-
      {
        "name":"Rémi1",
        "email":"remisalles1",
        "password":"aaaaaa"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
