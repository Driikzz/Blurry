info:
  name: UPDATE QUESTION
  type: http
  seq: 2

http:
  method: PUT
  url: "{{baseUrl}}/questions/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: multipart-form
    data:
      - name: statement
        type: text
        value: Quel est le nom de ce film (modifiéé) ?
      - name: response
        type: text
        value: Le silence des agneaux
      - name: file
        type: file
        value:
          - Images/tenet.jpg
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
