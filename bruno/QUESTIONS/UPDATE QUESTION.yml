info:
  name: UPDATE QUESTION
  type: http
  seq: 2

http:
  method: PUT
  url: "{{baseUrl}}/questions/:id"
  params:
    - name: id
      value: "9"
      type: path
  body:
    type: json
    data: |-
      {
        "statement": "Question ajoutés modifié",
        "response": "Réponse de la question ajoutée modifié"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
