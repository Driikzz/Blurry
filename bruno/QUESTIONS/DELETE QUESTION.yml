info:
  name: DELETE QUESTION
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/questions/:id"
  params:
    - name: id
      value: "9"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
