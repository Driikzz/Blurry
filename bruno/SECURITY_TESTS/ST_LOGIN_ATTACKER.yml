info:
  name: ST_LOGIN_ATTACKER
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "security.attacker@blurry.test",
        "password": "SecurityTest123"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("attacker logs in successfully, switching the active session", function () {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
