info:
  name: ST_LOGOUT
  type: http
  seq: 16

http:
  method: POST
  url: "{{baseUrl}}/auth/logout"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("logout clears the session before the unauthenticated tests", function () {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
