info:
  name: ST_USERS_UPDATE_FORBIDDEN_FOR_OTHER_USER
  type: http
  seq: 14

http:
  method: PUT
  url: "{{baseUrl}}/users/{{ownerUserId}}"
  body:
    type: json
    data: |-
      {
        "name": "Pwned",
        "email": "still-valid@blurry.test",
        "password": "whateverpassword"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("attacker cannot update another user's profile", function () {
          expect(res.getStatus()).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
