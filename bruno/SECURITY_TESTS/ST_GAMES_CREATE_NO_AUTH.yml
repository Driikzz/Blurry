info:
  name: ST_GAMES_CREATE_NO_AUTH
  type: http
  seq: 19

http:
  method: POST
  url: "{{baseUrl}}/games"
  body:
    type: json
    data: |-
      {
        "name": "Should never be created",
        "userIds": [{{ownerUserId}}],
        "quizId": {{ownerQuizId}}
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("creating a game without being authenticated is rejected", function () {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
