info:
  name: ST_USERS_UPDATE_NO_AUTH
  type: http
  seq: 17

http:
  method: PUT
  url: "{{baseUrl}}/users/{{ownerUserId}}"
  body:
    type: json
    data: |-
      {
        "name": "Pwned",
        "email": "still-valid@blurry.test",
        "password": "whateverpassword"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("updating a user without being authenticated is rejected", function () {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
