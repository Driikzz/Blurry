info:
  name: ST_GAME_DELETE_FORBIDDEN_FOR_OTHER_USER
  type: http
  seq: 13

http:
  method: DELETE
  url: "{{baseUrl}}/games/{{ownerGameId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("attacker cannot delete another user's game", function () {
          expect(res.getStatus()).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
