info:
  name: ST_QUIZ_DELETE_FORBIDDEN_FOR_OTHER_USER
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{baseUrl}}/quiz/{{ownerQuizId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("attacker cannot delete another user's quiz", function () {
          expect(res.getStatus()).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
