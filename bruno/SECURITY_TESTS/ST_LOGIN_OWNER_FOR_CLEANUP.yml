info:
  name: ST_LOGIN_OWNER_FOR_CLEANUP
  type: http
  seq: 21

http:
  method: POST
  url: "{{baseUrl}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "security.owner@blurry.test",
        "password": "SecurityTest123"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("owner logs back in to clean up test data", function () {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
